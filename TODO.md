# TODO

## Immediate

- [ ] Localhost preview workflow: use `npm run preview:local` to validate a production-like build (with Pagefind index) before any Cloudflare deploy.
- [ ] Populate at least 10 real project entries in `src/content/projects`.
- [ ] Replace placeholder screenshots and links with production assets.

## Cloudflare Pages

- [ ] Create Cloudflare Pages project connected to `SqualieC/SqualieC.github.io`.
- [ ] Set build command to `npm run build:search`.
- [ ] Set build output directory to `dist`.
- [ ] Configure custom domain and DNS records.
- [ ] Confirm preview deployments are enabled for non-main branches.
